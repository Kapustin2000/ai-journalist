{
  "name": "ai-journalist-root",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --dir back-end run start:dev",
    "dev:adk": "PORT=${ADK_PORT:-8000}; adk api_server --port $PORT",
    "dev:editor": "pnpm --dir asrp-editor run dev",
    "dev": "concurrently -k -p \"[{name}]\" -n back-end,adk,editor -c green,cyan,magenta \"pnpm run dev:backend\" \"pnpm run dev:adk\" \"pnpm run dev:editor\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}

